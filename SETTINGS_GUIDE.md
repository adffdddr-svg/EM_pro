# دليل صفحة الإعدادات
## Employee Management System

---

## 📋 نظرة عامة

تم إنشاء صفحة إعدادات شاملة تتيح إدارة جميع إعدادات النظام من مكان واحد.

---

## 🚀 خطوات التثبيت

### 1. إنشاء جدول الإعدادات

افتح هذا الرابط في المتصفح:
```
http://localhost/EM_pro/database/create_settings_table.php
```

أو نفذ ملف SQL مباشرة في phpMyAdmin:
```
database/settings_schema.sql
```

### 2. الوصول إلى صفحة الإعدادات

بعد تسجيل الدخول كمدير، اضغط على "الإعدادات" في القائمة الجانبية.

---

## 📑 التبويبات المتاحة

### 🌐 الإعدادات العامة
- اسم الموقع
- رابط الموقع
- وصف الموقع
- المنطقة الزمنية
- اللغة الافتراضية

### 📱 إعدادات العرض
- **الوضع الليلي** (Dark Mode) ⭐
- عدد العناصر في الصفحة
- صيغة التاريخ
- صيغة الوقت
- حجم الخط الافتراضي
- اللون الأساسي
- إظهار الإحصائيات
- إظهار الصور الشخصية

### 🔒 إعدادات الأمان
- الحد الأدنى لطول كلمة المرور
- انتهاء الجلسة
- تفعيل تسجيل الدخول بخطوتين
- عدد محاولات تسجيل الدخول الفاشلة
- مدة الحظر
- تفعيل HTTPS
- تفعيل حماية CSRF
- تفعيل حماية XSS
- تفعيل Remember Me

### 📅 إعدادات الإجازات
- الرصيد الافتراضي للإجازات السنوية
- الرصيد الافتراضي للإجازات الشهرية
- عدد أيام الإجازة المرضية المسموحة
- عدد أيام الإجازة الطارئة المسموحة
- تفعيل الإجازات غير المدفوعة
- عدد أيام الإشعار المسبق
- تفعيل نظام الموافقات المتعددة

### 🤖 إعدادات البوت الذكي
- تفعيل البوت الذكي
- مفتاح OpenRouter API
- النموذج المستخدم
- درجة الحرارة (Temperature)
- الحد الأقصى للرموز
- لغة البوت
- تفعيل الذاكرة
- عدد الرسائل المحفوظة

### 📧 إعدادات البريد الإلكتروني
- تفعيل إرسال الإيميلات
- SMTP Server
- SMTP Port
- SMTP Username
- SMTP Password
- نوع التشفير
- عنوان المرسل
- اسم المرسل

### 🔔 إعدادات الإشعارات
- تفعيل الإشعارات
- إشعارات إضافة موظف جديد
- إشعارات تعديل بيانات موظف
- إشعارات طلبات الإجازات
- إشعارات الموافقات/الرفض
- إشعارات انتهاء الجلسة
- إشعارات الأخطاء
- طريقة الإشعار

### 📁 إعدادات الملفات
- الحد الأقصى لحجم الملف
- أنواع الملفات المسموحة
- جودة ضغط الصور
- مكان حفظ الملفات
- تفعيل رفع الملفات
- مسح الملفات القديمة تلقائياً

---

## 🌙 الوضع الليلي (Dark Mode)

### المميزات:
- ✅ تبديل فوري بين الوضعين
- ✅ حفظ التفضيل في localStorage
- ✅ دعم تفضيلات النظام (System Preference)
- ✅ انتقالات سلسة
- ✅ يعمل على جميع الصفحات
- ✅ تصميم متجاوب

### كيفية الاستخدام:
1. اضغط على زر التبديل في أعلى الصفحة (☀️/🌙)
2. أو فعّله من صفحة الإعدادات → تبويب "العرض"

---

## 📁 الملفات المنشأة

```
admin/
├── settings.php              # صفحة الإعدادات الرئيسية
└── settings/
    └── save.php              # حفظ الإعدادات

database/
├── settings_schema.sql       # جدول الإعدادات
└── create_settings_table.php # إنشاء الجدول تلقائياً

assets/
├── css/
│   └── style.css            # (تم تحديثه - إضافة CSS للوضع الليلي)
└── js/
    └── dark-mode.js         # JavaScript للوضع الليلي

includes/
├── header.php               # (تم تحديثه - إضافة زر التبديل)
├── footer.php               # (تم تحديثه - إضافة JavaScript)
└── sidebar.php              # (تم تحديثه - إضافة رابط الإعدادات)
```

---

## 🔧 الاستخدام البرمجي

### الحصول على إعداد:
```php
// في أي صفحة
$db = getDB();
$stmt = $db->prepare("SELECT setting_value FROM settings WHERE setting_key = ?");
$stmt->execute(['site_name']);
$setting = $stmt->fetch();
$site_name = $setting['setting_value'];
```

### حفظ إعداد:
```php
$db = getDB();
$stmt = $db->prepare("INSERT INTO settings (setting_key, setting_value, setting_type, setting_group, updated_by) 
                     VALUES (?, ?, ?, ?, ?)
                     ON DUPLICATE KEY UPDATE 
                     setting_value = VALUES(setting_value)");
$stmt->execute(['site_name', 'اسم جديد', 'text', 'general', $_SESSION['user_id']]);
```

---

## ⚠️ ملاحظات مهمة

1. **الأمان**: جميع الإعدادات محمية بـ `requireAdmin()` - فقط المدير يمكنه الوصول
2. **الحفظ**: يتم حفظ التغييرات فوراً عند الضغط على "حفظ"
3. **النسخ الاحتياطي**: يُنصح بعمل نسخة احتياطية من جدول `settings` قبل التعديلات الكبيرة
4. **الوضع الليلي**: يتم حفظه في localStorage أولاً، ثم في قاعدة البيانات

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يظهر زر التبديل
**الحل**: تأكد من أنك مسجل دخول وأن ملف `dark-mode.js` محمّل

### المشكلة: الوضع الليلي لا يعمل
**الحل**: 
1. افتح Console في المتصفح (F12)
2. تحقق من وجود أخطاء JavaScript
3. تأكد من أن `window.SITE_URL` معرف في footer.php

### المشكلة: الإعدادات لا تُحفظ
**الحل**:
1. تحقق من وجود جدول `settings` في قاعدة البيانات
2. تحقق من صلاحيات قاعدة البيانات
3. راجع ملف `admin/settings/save.php` للأخطاء

---

## 📞 الدعم

للمساعدة والدعم، يرجى التواصل مع فريق التطوير.

---

**جامعة البصرة - كلية علوم الحاسوب وتكنولوجيا المعلومات**  
© 2024 - جميع الحقوق محفوظة

