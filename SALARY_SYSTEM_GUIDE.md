# دليل نظام تتبع الرواتب
## Employee Management System

---

## 📋 نظرة عامة

تم إنشاء نظام شامل لتتبع وإدارة الرواتب يتيح:
- تتبع تاريخ جميع التغييرات في الرواتب
- إحصائيات شاملة
- تقارير مفصلة
- سجل كامل لكل موظف

---

## 🚀 خطوات التثبيت

### 1. إنشاء جدول سجل الرواتب

افتح هذا الرابط في المتصفح:
```
http://localhost/EM_pro/database/create_salary_table.php
```

أو نفذ ملف SQL مباشرة في phpMyAdmin:
```
database/salary_schema.sql
```

### 2. الوصول إلى نظام الرواتب

بعد تسجيل الدخول كمدير، اضغط على "الرواتب" في القائمة الجانبية.

---

## 📑 الصفحات المتاحة

### 💰 قائمة الرواتب (`admin/salaries/index.php`)
- عرض جميع الموظفين مع رواتبهم
- إحصائيات فورية (إجمالي، متوسط، أعلى، أقل)
- بحث وفلترة حسب القسم
- ترتيب حسب الراتب أو الاسم
- روابط سريعة للعرض والتعديل

### ➕ إضافة/تعديل راتب (`admin/salaries/add.php`)
- اختيار الموظف
- إدخال الراتب الجديد
- تحديد تاريخ السريان
- اختيار سبب التغيير
- إضافة ملاحظات
- معاينة فورية للتغيير (المبلغ والنسبة)

### 👁️ عرض تفاصيل الراتب (`admin/salaries/view.php`)
- معلومات الموظف
- الراتب الحالي
- آخر تغيير
- سجل الرواتب (آخر 10 تغييرات)
- روابط سريعة للتعديل والسجل الكامل

### 📋 سجل الرواتب الكامل (`admin/salaries/history.php`)
- عرض جميع التغييرات في شكل Timeline
- تفاصيل كل تغيير (الراتب السابق، الجديد، المبلغ، النسبة)
- السبب والملاحظات
- تاريخ ووقت التغيير
- اسم المستخدم الذي أجرى التغيير

### 📊 إحصائيات الرواتب (`admin/salaries/statistics.php`)
- إحصائيات عامة (إجمالي، متوسط، أعلى، أقل)
- إحصائيات حسب القسم
- التغييرات الشهرية
- إحصائيات حسب نوع التغيير (زيادة، تخفيض، تعديل)
- فلترة حسب القسم والسنة

---

## 🗄️ قاعدة البيانات

### جدول `salary_history`

```sql
CREATE TABLE salary_history (
    id INT AUTO_INCREMENT PRIMARY KEY,
    employee_id INT NOT NULL,
    old_salary DECIMAL(10, 2) NULL,
    new_salary DECIMAL(10, 2) NOT NULL,
    change_type ENUM('increase', 'decrease', 'initial', 'adjustment'),
    change_amount DECIMAL(10, 2) NULL,
    change_percentage DECIMAL(5, 2) NULL,
    effective_date DATE NOT NULL,
    reason TEXT,
    notes TEXT,
    created_by INT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**الحقول:**
- `employee_id`: معرف الموظف
- `old_salary`: الراتب السابق
- `new_salary`: الراتب الجديد
- `change_type`: نوع التغيير (زيادة، تخفيض، ابتدائي، تعديل)
- `change_amount`: مبلغ التغيير
- `change_percentage`: نسبة التغيير
- `effective_date`: تاريخ السريان
- `reason`: سبب التغيير
- `notes`: ملاحظات
- `created_by`: المستخدم الذي أجرى التغيير

---

## 🔧 الدوال المساعدة

تم إضافة الدوال التالية في `includes/functions.php`:

### `addSalaryHistory()`
إضافة سجل راتب جديد في السجل.

### `getSalaryHistory()`
الحصول على سجل الرواتب لموظف معين.

### `getLastSalary()`
الحصول على آخر راتب لموظف.

### `updateEmployeeSalary()`
تحديث راتب موظف مع تسجيل تلقائي في السجل.

### `getSalaryStatistics()`
الحصول على إحصائيات الرواتب.

### `getSalaryChangeTypeText()`
تحويل نوع التغيير إلى نص عربي.

---

## 💡 المميزات

### ✅ تتبع كامل
- كل تغيير في الراتب يُسجل تلقائياً
- حفظ الراتب السابق والجديد
- حساب تلقائي للمبلغ والنسبة

### ✅ أنواع التغييرات
- **زيادة (increase)**: عندما يزيد الراتب
- **تخفيض (decrease)**: عندما ينخفض الراتب
- **ابتدائي (initial)**: أول راتب للموظف
- **تعديل (adjustment)**: تعديلات أخرى

### ✅ إحصائيات شاملة
- إجمالي ومتوسط الرواتب
- أعلى وأقل راتب
- إحصائيات حسب القسم
- تحليل التغييرات الشهرية

### ✅ واجهة سهلة
- تصميم احترافي ومتجاوب
- معاينة فورية للتغييرات
- بحث وفلترة متقدمة
- Timeline جميل للسجل

---

## 📁 الملفات المنشأة

```
admin/salaries/
├── index.php          # قائمة الرواتب
├── add.php            # إضافة/تعديل راتب
├── view.php           # عرض تفاصيل الراتب
├── history.php        # سجل الرواتب الكامل
└── statistics.php     # إحصائيات الرواتب

database/
├── salary_schema.sql              # جدول سجل الرواتب
└── create_salary_table.php        # إنشاء الجدول تلقائياً

includes/
└── functions.php      # (تم تحديثه - إضافة دوال الرواتب)

includes/
└── sidebar.php        # (تم تحديثه - إضافة رابط الرواتب)
```

---

## 🔒 الأمان

- ✅ حماية `requireAdmin()` - فقط المدير يمكنه الوصول
- ✅ التحقق من المدخلات
- ✅ استخدام Prepared Statements
- ✅ تسجيل المستخدم الذي أجرى التغيير

---

## 📊 الاستخدام

### إضافة راتب جديد:
1. اذهب إلى "الرواتب" → "إضافة راتب جديد"
2. اختر الموظف
3. أدخل الراتب الجديد
4. حدد تاريخ السريان
5. اختر السبب (اختياري)
6. أضف ملاحظات (اختياري)
7. اضغط "حفظ الراتب"

### عرض سجل موظف:
1. اذهب إلى "الرواتب"
2. اضغط "السجل" بجانب الموظف
3. أو اضغط "عرض" ثم "السجل الكامل"

### عرض الإحصائيات:
1. اذهب إلى "الرواتب" → "الإحصائيات"
2. اختر القسم والسنة (اختياري)
3. اضغط "تطبيق"

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يظهر جدول salary_history
**الحل**: نفذ `database/create_salary_table.php` أو `database/salary_schema.sql`

### المشكلة: لا يتم حفظ السجل
**الحل**: تحقق من:
- وجود جدول `salary_history`
- صلاحيات قاعدة البيانات
- وجود `created_by` في الجلسة

### المشكلة: الإحصائيات لا تظهر
**الحل**: تحقق من وجود موظفين نشطين في قاعدة البيانات

---

## 📞 الدعم

للمساعدة والدعم، يرجى التواصل مع فريق التطوير.

---

**جامعة البصرة - كلية علوم الحاسوب وتكنولوجيا المعلومات**  
© 2024 - جميع الحقوق محفوظة

